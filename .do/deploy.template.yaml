spec:
  name: absinthe-adapters
  services:
    - name: uniswapv2-indexer
      dockerfile_path: ops/dockerfiles/Dockerfile.uniswapv2
      github:
        repo: skushagra9/absinthe-adapters
        branch: test
        deploy_on_push: true
      health_check: null
      envs:
        - key: DB_NAME
          value: ${defaultdb.DATABASE}
        - key: DB_PORT
          value: ${defaultdb.PORT}
        - key: DB_HOST
          value: ${defaultdb.HOSTNAME}
        - key: DB_USER
          value: doadmin
        - key: DB_PASS
          value: ${defaultdb.PASSWORD}
        - key: NODE_TLS_REJECT_UNAUTHORIZED
          value: "0"

  databases:
    - name: defaultdb
      engine: PG
      version: "17"
